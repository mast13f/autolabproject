# AutoLab dependencies
# Install with:  pip install -r requirements.txt
#
# NOTE: super-gradients pins numpy<2 and torch. If you only need the
#       campaign / dashboard / OT-2 control and NOT the YOLO inference,
#       omit the super-gradients block below.

# ── Numerical / optimisation ──────────────────────────────────────────────────
numpy==1.26.4           # array ops; pinned to <2 for super-gradients compatibility
scipy>=1.11             # Gaussian Process optimisation, Latin Hypercube Sampling
scikit-learn>=1.3       # GaussianProcessRegressor, Matern / RBF kernels

# ── Plotting ──────────────────────────────────────────────────────────────────
matplotlib>=3.7         # convergence plot saved to PNG (Agg backend, no display needed)

# ── Computer vision ───────────────────────────────────────────────────────────
opencv-python>=4.8      # camera capture (camera_server.py) and image processing

# ── YOLO-NAS inference (liquid detection) ─────────────────────────────────────
# Requires a CUDA-capable GPU for reasonable speed; CPU inference is supported
# but slow. super-gradients pulls in torch automatically.
super-gradients>=3.7    # YOLO-NAS model loading (camera/analysis_script/liquid_analysis.py)

# ── Standard library (no install needed) ─────────────────────────────────────
# argparse, ast, csv, dataclasses, datetime, http.server, itertools, json,
# os, pathlib, random, re, shutil, socket, statistics, subprocess, sys,
# threading, time, typing, urllib, warnings, webbrowser
