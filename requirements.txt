# AutoLab dependencies
# Install with:  pip install -r requirements.txt

# ── Numerical / optimisation ──────────────────────────────────────────────────
numpy>=1.24             # array ops
scipy>=1.11             # Gaussian Process optimisation, Latin Hypercube Sampling
scikit-learn>=1.3       # GaussianProcessRegressor, Matern / RBF kernels

# ── Plotting ──────────────────────────────────────────────────────────────────
matplotlib>=3.7         # convergence plot saved to PNG (Agg backend, no display needed)

# ── Computer vision ───────────────────────────────────────────────────────────
opencv-python>=4.8      # camera capture (camera/server.py) and image subtraction analysis

# ── Data analysis (optional — only used by MetaModel for cross-run learning) ─
pandas>=2.0             # optimizer/meta_model.py — cross-run transfer learning

# ── YOLO-NAS inference (legacy — no longer used by the main pipeline) ────────
# The main image analysis now uses OpenCV-only image subtraction (autolab/image_analysis.py).
# super-gradients + YOLO-NAS weights are only needed if you want to run the old
# camera/analysis.py pipeline independently. Uncomment the line below if needed:
# super-gradients>=3.7

# ── Standard library (no install needed) ─────────────────────────────────────
# argparse, ast, csv, dataclasses, datetime, http.server, itertools, json,
# os, pathlib, random, re, shutil, socket, statistics, subprocess, sys,
# threading, time, typing, urllib, warnings, webbrowser
